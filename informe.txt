Los comandos ejeucutados estan en cli.txt

Se realizo un test con artillery, el resultado de artillery esta en el archivo result.txt, y el resultado de node profiler esta en artilleryTestResults.

Se realizo un test con autocannon a traves del codigo en el archivo autocannonBenchmark, los resultados estan en la imagen autocannonResult.png.

Se reviso el perfilamiento del servidor con el modo inspector de node.

Se realido un diagrama de flama con 0x, emulando carga con autocannon en el archivo autocannonBenchmark. El resultado esta en la carpeta 6748.0x.

La conclusion es que el servidor responde mucho mejor en modo cluster y con la menor cantidad de procesos bloqueantes.